# 🧸 Проста інструкція: Як навчити сервер оновлюватися самому

Ми зробимо так, щоб сайт оновлювався автоматично, коли ви відправляєте код на GitHub.

---

### Крок 1: Готуємо сервер (лише один раз)
1. Підключіться до вашого сервера (як ви зазвичай це робите).
2. Напишіть команду: `nano /opt/police-feedback/deploy.sh`
3. У чорне вікно, що відкрилося, вставте цей текст:
   ```bash
   #!/bin/bash
   cd /opt/police-feedback
   git pull origin main
   npm install
   npm run build
   sudo systemctl restart police-feedback
   ```
4. Натисніть на клавіатурі: `Ctrl` + `O`, потім `Enter`, потім `Ctrl` + `X`.
5. Тепер напишіть останню команду на сервері:
   `chmod +x /opt/police-feedback/deploy.sh`

---

### Крок 2: Створюємо "ключик" (щоб GitHub міг зайти на сервер)
1. У себе на **ПК** (не на сервері) відкрийте термінал і напишіть:
   `ssh-keygen -t ed25519`
   *Просто натискайте `Enter`, поки воно не закінчить.*
2. Тепер нам треба дати "замок" серверу:
   Напишіть: `ssh-copy-id root@ВАША_АДРЕСА_СЕРВЕРА`
   *Це дозволить заходити без пароля.*

---

### Крок 3: Даємо "ключик" Гітхабу
1. На вашому **ПК** напишіть команду, щоб побачити ваш секретний ключ:
   `cat ~/.ssh/id_ed25519`
2. Виділіть та скопіюйте весь текст, що з'явився (він починається з `-----BEGIN OPENSSH PRIVATE KEY-----`).
3. Зайдіть на сайт **GitHub** у ваш проєкт.
4. Натисніть зверху **Settings** (Налаштування).
5. Зліва знайдіть **Secrets and variables** -> **Actions**.
6. Натисніть кнопку **New repository secret** (Новий секрет).
7. У поле **Name** напишіть: `SSH_PRIVATE_KEY`
8. У поле **Secret** вставте той довгий текст, що ви скопіювали.
9. Натисніть **Add secret**.

**Так само додайте ще два секрети:**
*   `REMOTE_HOST` — сюди вставте IP-адресу вашого сервера.
*   `REMOTE_USER` — сюди напишіть `root`.

---

### Крок 4: Запускаємо диво!
Тепер просто відправте нові файли (які я створив) на GitHub. Напишіть у терміналі на ПК:
```bash
git add .
git commit -m "Налаштував авто-оновлення"
git push origin main
```

**Все!** Тепер щоразу, коли ви будете робити `git push`, сервер сам забере код, збере його і перезапустить сайт. Ви зможете бачити, як він це робить, у вкладці **Actions** на GitHub.
